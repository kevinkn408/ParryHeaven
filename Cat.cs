using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Cat : MonoBehaviour
{
    private string projectileTrigger;
    private string projectileSpecialTrigger;
    private string superTrigger;
    private string hurtAnimationClip;
    private string projectileClip;
    private string justParryClip;
    private string parryClip;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
